/**
 * Pre-merge Dashboard — REMOVED.
 * 
 * Pre-merge is now fully on-demand during annotation viewing.
 * This component is kept as a stub to prevent import errors.
 */
export function PremergeDashboardPage() {
  return (
    <div className="p-8 text-center text-slate-500">
      <p>Pre-merge dashboard telah dihapus.</p>
      <p className="text-sm mt-1">Merge sekarang dilakukan on-demand saat annotator membuka kalimat.</p>
    </div>
  );
}
